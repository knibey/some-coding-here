#include <stdio.h>

int main() {
	int num, base, power, powerbase;
	int counter = 1;

	scanf("%d %d", &num, &base);

	power = num;

    for ( int i = ; i >= 0; i-- ) {

    }
    printf("\n");

	return 0;
}
